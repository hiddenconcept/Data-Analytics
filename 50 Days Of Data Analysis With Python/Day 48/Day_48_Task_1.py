#1 When training a model on a dataset,
# it is advisable to split the data into training and test sets.
# Why do we split data into training and test sets?

print("We split data into training and test sets so the model can learn from the training data and then be evaluated on unseen test data.\nThis provides an unbiased measure of its performance and helps ensure it generalizes well instead of simply memorizing the training data.\n")